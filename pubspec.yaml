name: path_animation
description: A Flutter package for easy to animated the widget cross the path.
version: 1.0.2
homepage: https://github.com/haishuangsu/path_animation.git

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
  
environment:
  sdk: '>=3.2.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
